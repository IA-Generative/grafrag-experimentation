# Corpus Input

Place Markdown, text, or JSONL source files in this directory before running indexing.

The repository ships with a tiny sample corpus so local smoke tests have something to rank.

For a richer demo corpus based on French Wikipedia pages about medieval wars between England and France, run:

```bash
python3 scripts/generate_medieval_wars_corpus.py --clean
```
