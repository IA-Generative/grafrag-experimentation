# Pipelines

The pipeline module in this directory is loaded by the `ghcr.io/open-webui/pipelines` container.

It forwards prompts to the FastAPI bridge and exposes two variants:

- `graphrag-local`
- `graphrag-global`

